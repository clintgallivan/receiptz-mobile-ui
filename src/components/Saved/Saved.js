import React, { useCallback, useEffect } from 'react';
import {
  View,
  Text,
  TouchableOpacity
} from 'react-native';

const Saved = () => {
  return (
  <View>
    <Text>Saved Screen</Text>
  </View>
  )
};


export default Saved;